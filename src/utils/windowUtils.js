// Deprecated: re-exporting from centralized `entityUtils` to avoid duplication.
import {
  WINDOW_DIRECTIONS,
  snapToGrid,
  isOnWall,
  getWindowRotation,
  updateWindowPosition,
} from "./entityUtils";

export {
  WINDOW_DIRECTIONS,
  snapToGrid,
  isOnWall,
  getWindowRotation,
  updateWindowPosition,
};
